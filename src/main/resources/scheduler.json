[
	{
		"name" : "BirthdayScheduler",
		"time" : "15 * * * * *",
		"type" : "BirthdayScheduler",
		"data" : "SELECT  * FROM EMPLOYEES WHERE MONTH(TO_DATE(DOB, 'DD/MM/YYYY')) = MONTH(CURRENT_DATE) AND DAYOFMONTH(TO_DATE(DOB, 'DD/MM/YYYY')) = DAYOFMONTH(CURRENT_DATE)"
	}
]